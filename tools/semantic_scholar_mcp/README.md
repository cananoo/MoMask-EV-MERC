# Semantic Scholar MCP

Project-local MCP server for academic literature lookup.

Run:

`python tools/semantic_scholar_mcp/server.py`

Optional environment variable:

`SEMANTIC_SCHOLAR_API_KEY`

Provided tools:

- `search_papers(query, year_from=2024, year_to=2026, limit=10)`
- `get_paper(identifier)`
